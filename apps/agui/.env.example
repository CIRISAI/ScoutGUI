# Scout GUI Environment Configuration

# Scout API Configuration (Cloudflare-hosted)
# The base URL for the Scout agent API
NEXT_PUBLIC_SCOUT_API_URL=https://scoutapi.ciris.ai
NEXT_PUBLIC_API_BASE_URL=https://scoutapi.ciris.ai

# Scout API Port (if needed for local development)
NEXT_PUBLIC_SCOUT_API_PORT=8080

# OAuth Configuration (Production)
# These should be configured in your OAuth provider settings
# Callback URLs should be: https://yourdomain.com/oauth/scout/{provider}/callback
NEXT_PUBLIC_OAUTH_CLIENT_ID=your-oauth-client-id
NEXT_PUBLIC_OAUTH_REDIRECT_URI=https://yourdomain.com/oauth/scout/google/callback

# Application Configuration
NODE_ENV=production

# Optional: Analytics and Monitoring
# NEXT_PUBLIC_ANALYTICS_ID=your-analytics-id

# Optional: Sentry Error Tracking
# NEXT_PUBLIC_SENTRY_DSN=your-sentry-dsn
